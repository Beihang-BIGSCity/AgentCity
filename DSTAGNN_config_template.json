{
  "model_name": "DSTAGNN",
  "task": "traffic_state_pred",

  "_comment_data": "Data window configurations",
  "input_window": 12,
  "output_window": 12,

  "_comment_architecture": "Model architecture parameters",
  "nb_block": 4,
  "K": 3,
  "in_channels": 1,

  "_comment_filters": "Convolution filter sizes",
  "nb_chev_filter": 32,
  "nb_time_filter": 32,

  "_comment_attention": "Attention mechanism parameters",
  "d_model": 512,
  "d_k": 32,
  "n_heads": 3,

  "_comment_graph": "Graph configuration - use 'AG' for adaptive graph or 'G' for static",
  "graph_use": "AG",

  "_comment_training": "Training hyperparameters",
  "learning_rate": 0.001,
  "batch_size": 32,
  "epochs": 100,
  "optimizer": "Adam",
  "weight_decay": 0.0001,

  "_comment_scheduler": "Learning rate scheduler",
  "lr_scheduler": "MultiStepLR",
  "lr_decay_ratio": 0.1,
  "steps": [50, 80],

  "_comment_evaluation": "Evaluation metrics",
  "metrics": ["MAE", "RMSE", "MAPE"],
  "loss_func": "masked_mae",

  "_comment_device": "Computation device",
  "device": "cuda",
  "gpu_id": 0,

  "_comment_logging": "Logging and checkpointing",
  "log_level": "INFO",
  "saved_model": true,
  "save_epoch": 10,

  "_comment_reduced": "===== REDUCED CONFIGURATION FOR LIMITED MEMORY =====",
  "_reduced_nb_block": 2,
  "_reduced_d_model": 256,
  "_reduced_nb_chev_filter": 16,
  "_reduced_nb_time_filter": 16,
  "_reduced_batch_size": 16,

  "_comment_notes": [
    "This is a template configuration for DSTAGNN model in LibCity framework",
    "Uncomment and use the _reduced parameters if running on limited GPU memory",
    "The model expects the following data_feature keys:",
    "  - num_nodes: number of vertices in the graph",
    "  - adj_mx: static adjacency matrix (N, N)",
    "  - adj_TMD: dynamic graph (STAG/STRG) - optional, defaults to adj_mx",
    "  - adj_pa: pre-defined adjacency - optional, defaults to adj_mx",
    "  - scaler: data normalization scaler",
    "Minimum input_window is 5 due to GTU fusion layer constraints",
    "For best results, input_window and output_window should be >= 12"
  ]
}
